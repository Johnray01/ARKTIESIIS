# Goal Plan: Phases 8–15

The development roadmap marks Phases 1–7 complete. This plan is split into two separate Goal Mode runs in the existing roadmap order. Goal 1 covers Phases 8–10: secure document management, local OCR, and validation/review. Goal 2 covers Phases 11–15 and may start only after Goal 1 is complete. Phase 10 now has thesis-owned operational review defaults for the advisory clues, required manual source-inspection checklist, and student-facing status labels. Passing the Phase 10 thesis implementation gate means those controls, persistence, role protections, and tests work as specified here; it does not mean the institution has approved the criteria. A separate school-adoption gate remains pending institution signoff before school deployment. Keep each phase reviewable and create one local commit for each completed phase after its acceptance gate passes; never push. The Phase 9 native-runtime gate is a real smoke run on the local Linux environment; Windows path-handling unit tests and the historical Windows report are compatibility evidence only. Use the same local checkout for both runs so the docs and completed work remain available. Do not overlap the goals or begin Goal 2 early.

## Current repository state

- `database/schema.sql` defines `documents` and `document_validations`, the four paper-listed document types, file metadata, uploader, processing/review fields, and status constraints. The baseline is version `001`; migrations `002`–`005` add two-factor limits, document revisions/review events, processing leases, and the local OCR default while preserving existing validation rows. Migration `006` adds immutable digital review decisions and a separate Form 137 physical-status history; `007` adds student LRN and private grade-import preview storage; `008` adds nullable structured staff verification-checklist storage while leaving earlier decisions unknown. Schema changes use new numbered, forward-only migrations.
- `src/config/environment.js` reads configurable Tesseract/Poppler executable paths, English OCR language, a bounded timeout, bounded worker concurrency, and `MAX_UPLOAD_MB` (default 10 MB).
- `src/services/localOcrService.js` securely copies a private upload into a temporary directory, uses `pdfinfo` and page-by-page `pdftoppm` rendering for PDFs, and calls Tesseract with native `execFile` argument arrays. `src/services/documentProcessingService.js` atomically claims pending immutable submissions, bounds concurrency/time/output/page count, recovers stale processing leases to a safe failed result, normalizes OCR outcomes, and stores a per-submission result. `src/server.js` starts the bounded queue/recovery scan after database connection and repeats it periodically. `src/services/documentValidationService.js` provides advisory OCR clues for staff inspection; it does not implement institution-approved validation rules.
- `src/services/form137ScanService.js` handles staff-only physical Form 137 scans separately from stored uploads. Scan bytes are sent to the server for local processing. It validates the same file metadata/signature/size rules, bounds active scans, stages to a private temporary directory, clears the multipart buffer, and removes the temporary directory after success or failure. Only bounded student-name/school-name suggestions appear in a private no-store response; nothing from the scan is retained as a document or database record.
- Phase 8 connects upload, authorized private download, per-student document history, immutable corrected re-upload, review handoff, and correction requests. Phase 9 connects OCR and staff-only extraction display. Phase 10 uses thesis-owned advisory and manual source-inspection defaults; passing its implementation gate is separate from institution signoff for school adoption.
- The existing role, CSRF, SQL, and audit patterns in the application should be followed. Do not change the one-time schema baseline to update an initialized database.

## Role and document access matrix

All permissions must be enforced on server routes and every file download. Student identifiers supplied in a URL or form must never substitute for the authenticated student's own record link.

| Role | Good Moral Certificate | Report card | Form 137 | PSA birth certificate |
| --- | --- | --- | --- | --- |
| `student` | Upload, view, check status, and re-upload for own record only | Upload, view, check status, and re-upload for own record only | View own physical status and staff instruction; no scan, OCR, or file access | View/download only a staff-uploaded file for own record; no upload |
| `registrar` | Manage, manually review, and download authorized student records | Manage, manually review, and download authorized student records | Scan physical paper for transient suggestions; record status/instruction; historical files remain staff-only | Upload, manage, manually review, and download authorized student records |
| `database_admin` | Oversee, manage, manually review, and download | Oversee, manage, manually review, and download | Scan physical paper for transient suggestions; record status/instruction; historical files remain staff-only | Upload, oversee, manage, manually review, and download |
| `finance` | No access | No access | No access | No access |

Students may upload only Good Moral Certificates and report cards. “Manage” permits the authorized staff workflow for a school record; it does not grant a student access to another student's records. Form 137 status history records a human decision and instruction; a missing staff event displays “Not recorded.” Staff send scan bytes to a CSRF-protected endpoint for temporary processing; the scan is not retained as an uploaded document and no OCR result is saved. Keep historical Form 137 files staff-only. Record material upload, re-upload, review decision, and status change through audit events without logging document contents, extracted text, instructions, secrets, or raw command/database errors.

## Phase 8 — Document Management

1. Inspect the current document schema and application conventions before changing them. Confirm the upload size limit, storage location, and any institution-approved retention requirements; do not invent retention or naming policy.
2. Add role-protected upload, list/detail, download, review handoff, and re-upload routes and matching views. Students upload their own Good Moral Certificates and report cards. Staff upload those types and PSA birth certificates. Form 137 has no persistent file upload, but staff may temporarily scan its physical paper for OCR suggestions. Use CSRF protection for mutations and repeat active-role and ownership checks inside service writes where the existing pattern does so.
3. Accept only PDF, JPEG, and PNG. Validate the extension, declared MIME type, actual file signature where applicable, and configured size limit on the server. Reject missing, empty, oversized, or mismatched uploads with a user-safe message.
4. Generate opaque stored names. Store files outside the public directory and resolve every path beneath the configured storage root. Never render a filesystem path. Download handlers must recheck role and record ownership before streaming a file.
5. Persist the existing metadata: student, selected document type, original filename, opaque stored filename, MIME type, byte size, uploader/source, status, and timestamp. Clean up an unreferenced file if the database write fails; report a safe error if cleanup also fails.
6. Treat each corrected digital upload as a new immutable submission. Students may re-upload only their own Good Moral Certificates and report cards; registrar/database administrators may submit linked corrections for Good Moral Certificates, report cards, and PSA certificates. Preserve the previous file and validation history. Form 137 changes append a physical status event and never create or replace a file.
7. Build student history/status views and staff search/review views under the role matrix above. Students may view/download their own staff-uploaded PSA certificate. Do not expose OCR output or staff-only decision notes to students.

### Phase 8 acceptance checks

- Students can upload/retrieve only their own Good Moral Certificates and report cards and can view/download only their own staff-uploaded PSA certificate. Attempts to change a student ID, URL, or document ID cannot reveal another student's file or a historic Form 137 file.
- Staff can upload Good Moral Certificates, report cards, and PSA certificates. Staff can temporarily scan physical Form 137 paper and record its status; no scan file or OCR result is stored, and historical files are staff-only. Finance receives no document access.
- Disallowed types, MIME/extension mismatches, empty files, invalid file signatures, and size-limit violations are rejected. Stored files are not served from the public static directory.
- A failed database write does not leave an untracked file. An unauthorized or stale download request is denied.
- Re-upload creates a new submission while prior files, statuses, validation results, and review history remain available to authorized staff.
- Mutations use CSRF checks, parameterized SQL, and audit events consistent with existing application patterns.

## Phase 9 — Local OCR with Tesseract and Poppler

1. Require a successful real native OCR run on the local Linux environment, using Tesseract with English trained data and Poppler's `pdfinfo`/`pdftoppm`. Configure executable paths, language, timeout, and concurrency through runtime environment settings. Windows paths may contain spaces and must be passed directly without a shell; their mocked path-handling tests are compatibility coverage and do not replace the Linux run.
2. Keep uploads in private storage. Copy a verified regular file to a private temporary directory, call Tesseract directly for JPEG/PNG, and use `pdfinfo` then page-by-page `pdftoppm` rendering plus Tesseract for PDFs. Clean rendered pages immediately and always remove the private temporary directory.
3. Enforce a technical 20-page PDF maximum (`OCR_MAX_PDF_PAGES` may lower it), bounded render dimensions/output, a bounded timeout that aborts active subprocesses, and bounded worker concurrency. Preserve the existing configurable 10 MB upload default.
4. Upload handlers schedule background work and return without waiting for OCR. Startup and periodic scans recover stale leases and drain pending digital rows. Historical Form 137 rows are excluded from OCR claim and recovery queues; temporary physical scans bypass those queues. Claim one row atomically under SQL Server read-committed locking so parallel workers do not process a submission twice.
5. Persist normalized extraction results in `document_validations`; limit access to extracted text because it contains student information. Handle empty OCR, malformed/encrypted/over-limit PDFs, missing tools, timeout, and processing errors with fixed safe messages. Do not save partial PDF text as a completed result or expose raw command/database errors.
6. Do not classify documents or infer authenticity. OCR output is untrusted input: escape it in views, do not execute it, and pass only the uploader-selected type to advisory checks. Store possible school-name candidate lines as bounded, escaped staff suggestions. Report-card OCR does not suggest or write numeric grades. Authorized staff can enter grades through the existing academic-record workflow, and the registrar-reviewed corrected SSHS E-Class Record import described below is another grade source.

### Phase 9 acceptance checks

- Valid synthetic image/PDF fixtures reach installed Linux Tesseract and Poppler through `npm run ocr:smoke`; extracted phrases, two-page order, and private temporary-file cleanup pass. This is the native-runtime acceptance check for Phase 9. The mocked Windows executable-path test remains compatibility evidence only, and the historical Windows report is not a current Linux acceptance result.
- The opt-in native service-integration check, `ARKTIESIIS_RUN_NATIVE_OCR_INTEGRATION=1 node --test tests/documentProcessing.test.js`, passes on Linux in addition to `npm run ocr:smoke`. It exercises a synthetic digital upload through private storage and processing with real local OCR, verifies its validation remains linked to that submission and staff-only, and checks Form 137 scan cleanup. It uses injected in-memory SQL harnesses, so it does not validate live database behavior or the authenticated route end to end.
- `npm test`, `npm run check`, and `npm run db:check` pass on the current checkout. Run `npm run db:migration:rollback-test` only when migration 006 and its target tables are absent; if 006 is already installed, record the script's guarded refusal and leave the initialized database unchanged. Never apply or revert an installed migration to force this check to run.
- PDF pages preserve input order; page, file, output, timeout, and concurrency limits are enforced; subprocesses are cancelled on timeout and temporary files are removed on success and failure.
- Concurrent workers claim distinct pending rows; upload responses do not wait for OCR; startup and periodic recovery handle stale leases.
- Missing binaries, malformed/encrypted/over-limit PDFs, timeouts, empty OCR, and tool errors are recorded safely and do not leave a document indefinitely marked as processing.
- Extracted text and validation details are inaccessible to unauthorized roles and are rendered safely when shown to authorized staff.
- No OCR result or UI claims authenticity, forgery detection, signature/seal verification, forensic review, or automatic type classification.

## Phase 10 — Advisory Checks and Human Review

1. The current implementation surfaces a linked student-name OCR clue for report cards, Good Moral Certificates, and PSA birth certificates, plus possible school-name lines for report cards and Good Moral Certificates. The PSA clue is limited to student-name text. Form 137 has no school-name requirement; its temporary scan may return a possible school-name line as optional advice. Report-card OCR does not suggest or write grade entries. Authorized staff may enter academic grades through the existing academic-record workflow, and the registrar-reviewed corrected SSHS E-Class Record import is another grade source; neither defines Phase 10 document-validation criteria. These suggestions do not define pass/fail criteria, and no school approval is inferred.
   These clues are advisory. A student-name match does not establish ownership, and a possible school-name line does not establish school-name legitimacy.
2. Before recording `verified` for a report card or Good Moral Certificate, a registrar/database administrator must attest after source inspection that the linked student name and school-name text are visible and legible, the selected type matches the file, and all submitted pages are readable and complete. For PSA, the same checklist applies without the school-name item. These are thesis-owned staff review controls, not OCR checks, institution-defined page requirements, school approval, or proof of authenticity. The checklist is stored with the immutable decision; pre-migration-008 decisions remain unknown. Checklist details are staff-only.
3. Require registrar/database administrator source inspection after OCR and record one immutable manual decision: verify, correction request, or reject. Only manual verification sets `documents.status` to `valid`. A reason is required to verify despite OCR failure or a missed advisory clue. Do not use a whitelist, grade threshold, automated completeness/format rule, or automatic acceptance. Staff may choose correction or rejection using the existing specific instruction/reason controls; the system does not select an outcome when staff cannot confirm a field.
4. Students see only operational status and correction instructions, not OCR text, checklist details, or internal override notes. Status labels are “Waiting for OCR,” “OCR processing,” “OCR could not process; staff review required,” “Awaiting staff review,” “Correction requested,” “Verified after staff source inspection,” and “Rejected after staff review.” These describe workflow state only and do not imply institution approval or document authenticity.
5. Form 137 uses a separate physical status history with pending, received, verified, correction, and rejected outcomes. “Not recorded” is shown until staff add an event. Registrar/database administrator users can scan the paper for temporary OCR suggestions; the endpoint is CSRF-protected, returns a no-store response, and persists no scan or OCR data. A failure is shown to staff and does not prevent a manual status decision; historical files remain staff-only.
6. Keep decision/status events append-only and auditable. Use document row locks and status checks so OCR completion or recovery cannot overwrite a manual decision. Preserve all historical OCR rows and review history.

### Phase 10 acceptance checks

- **Thesis implementation gate:** role restrictions, advisory clue behavior, required type-specific staff checklist, server-side rejection of missing/tampered attestations, override reasons, immutable checklist/decision history, concurrency, staff-only history, student-facing wording, and Form 137 transient-scan/status behavior are covered by automated tests. Migration 008 preserves older checklist state as unknown. The gate passes only after required test/check commands succeed and the database check confirms migration 008.
- **School-adoption gate (pending):** institution signoff is still required before school deployment. The bounded first/last-name OCR clue does not implement an approved identity rule; middle names, aliases, and OCR errors can produce missed clues. Visible school-name text does not verify school legitimacy. The checklist's submitted-page observation does not define required pages. Accepted school-name variants, institution-required document pages, and any future format/compliance criteria remain unspecified. Form 137's possible-school line is optional advice and not a requirement. No grade threshold, school-name whitelist, or automatic acceptance is implemented; numeric grades are not extracted from report-card OCR.
- Current student wording is thesis-selected workflow text only: “Waiting for OCR,” “OCR processing,” “OCR could not process; staff review required,” “Awaiting staff review,” “Correction requested,” “Verified after staff source inspection,” and “Rejected after staff review.” The terms “verified” and “rejected” record staff workflow decisions and do not claim authenticity or institution approval.
- `valid` is written only by manual verification; OCR success and advisory matches do not auto-accept a document. Finance cannot access document routes or APIs.
- A corrected digital upload starts a new OCR run without changing the prior OCR result or decision history.

## Phase 11 — Dashboards and Reporting

Build the student, registrar, finance, and database administrator dashboards from the role-specific capabilities already in scope. Add useful counts and status summaries only; do not add analytics or reports that require new school policy or expose data across roles.

**Acceptance gate:** each role sees the expected workspace and summaries; students see only their own document/academic information, finance sees finance data only, and no dashboard action bypasses server authorization.

## Phase 12 — Security and Audit

Review authorization, input validation, upload hardening, session protections, audit coverage, and sensitive data exposure across the completed workflows. Address findings in the existing stack and document backup/restore procedures needed before deployment.

**Acceptance gate:** server-side role and ownership checks cover protected reads and writes; upload and download paths remain private; important actions are auditable without storing secrets or raw document contents; production errors do not expose SQL, stack traces, or native utility details; security checks pass.

## Phase 13 — Testing

Complete the roadmap test coverage for validation rules, authentication and roles, CRUD workflows, document upload/re-upload/download, and the manual role/access matrix. Run the relevant automated checks and an end-to-end workflow against a configured environment where available; record any environment-dependent checks that could not run.

**Acceptance gate:** automated tests and project checks pass; manual role/access scenarios pass; each document lifecycle path has verified success and safe failure behavior; remaining failures or unavailable dependencies are documented before UI polish or deployment work.

## Phase 14 — UI Polish

Polish responsive layouts, empty/loading/error states, form feedback, and accessibility basics across the completed role workflows. Keep labels and status wording understandable without implying that OCR proves authenticity.

**Acceptance gate:** core screens remain usable at narrow and desktop widths; forms and status changes provide clear feedback; keyboard navigation, visible focus, labels, and error messages work across the main flows; no role-specific information is exposed by presentation changes.

## Phase 15 — Deployment and Defense Readiness

The future deployment target is a Hostinger KVM 2 VPS using its plain Ubuntu 22.04 image. Hostinger currently lists plain Ubuntu 22.04 among its VPS operating-system templates ([available templates](https://www.hostinger.com/support/1583571-what-are-the-available-operating-systems-for-vps-at-hostinger/)). Keep the existing Node.js 20+, Express, EJS, SQL Server, local Tesseract, and Poppler stack. The eventual VPS must run local OCR binaries and English data, keep uploaded documents in private persistent storage outside the public application tree, and use a SQL Server edition/license that permits the intended production workload. SQL Server 2022 on Ubuntu 22.04 requires CU 10 or later ([Microsoft support matrix](https://learn.microsoft.com/en-us/sql/linux/sql-server-linux-whats-new-2022?view=sql-server-ver17)); the repository's local SQL Server Developer container is for development/testing and is not licensed for production ([Microsoft SQL Server licensing guidance](https://www.microsoft.com/licensing/guidance/SQL)).

For this Goal 2 run, Phase 15 is **preparation only**. Prepare and review the target configuration, deployment runbook, backup/restore procedure, unresolved-security checklist, and defense checklist. Do not provision the VPS, purchase or apply a production SQL Server license, run a production migration, or exercise restore against a production database. A procedure in a runbook is not evidence that its operation was exercised.

**Phase 15 preparation acceptance gate:** the proposed Hostinger KVM 2/plain Ubuntu 22.04 target and support/licensing checks are documented; production configuration and private-storage expectations are described; paired database/upload backup and isolated restore steps are reviewable; open SEC findings are carried forward; the demo seeder's development/loopback protection and rerun behavior are covered by tests; and the defense checklist respects the role matrix, OCR advisory scope, and objective gaps. Local tests/checks may support this preparation gate but do not establish live deployment readiness.

**Live-deployment gate — PENDING:** the actual Hostinger plan and Ubuntu support path are verified and provisioned; production SQL Server uses a supported Ubuntu 22.04 CU 10+ build and an appropriately licensed edition; secrets, proxy, sessions, rate limits, SMTP, SQL TLS, and private persistent uploads are configured and verified; production migrations are approved and applied; real native OCR and role/private-download checks pass on the VPS; and a paired backup has passed a full isolated restore drill. No provisioning, production migration, restore drill, or live deployment has been performed in this preparation phase. Do not treat the local Developer container, local smoke checks, or a plan description as production evidence.

**School-adoption gate — PENDING:** obtain institution signoff and approved requirements/decision wording for school use. This remains separate from both the Phase 15 preparation gate and the live-deployment gate; completing either does not establish school approval.

## Stop conditions

- **Goal 1:** Run Phases 8–10 in order within one Goal Mode objective. After each phase passes its thesis implementation gate, report a concise checkpoint and create one local commit for that completed phase. Phase 9 requires the agreed local checks and a successful real native OCR smoke on Linux. Windows path-handling tests and the historical Windows report are compatibility evidence only, not an alternate or additional runtime gate. Phase 10 uses the thesis-owned operational defaults documented above; its implementation gate can pass when code, persistence, role protections, and checks pass. Keep the separate school-adoption gate pending until institution signoff; do not describe thesis defaults as institution-approved or deploy them as school policy before signoff. Never push. Stop after Phase 10 passes its thesis implementation gate and report school adoption as pending. Do not start Phase 11 or any later phase in this goal.
- **Goal 2:** Start a separate Goal Mode objective after Goal 1 has completed Phases 8–10, passed the Phase 10 thesis implementation gate, and stopped. Begin Phase 11 in the same local checkout and run Phases 11–15 continuously and in order. The pending school-adoption gate does not prevent local work, but do not represent thesis defaults as approved school policy. After each phase passes its acceptance gate, report a concise checkpoint, create one local commit for that completed phase with a plain human message, and continue to the next phase automatically. Do not automatically provision, purchase, migrate a production database, or deploy. Pause at Phase 15 if real external prerequisites or an irreversible action prevent the gate from being completed safely. Stop after Phase 15 passes its gate and report Goal 2 complete.
- Never push. If a gate fails, resolve it within the current phase before proceeding. Pause only when the current gate depends on user input, required institution-approved rules or credentials are missing, or an irreversible action requires approval. Pending school-adoption signoff alone does not block local work in Goal 2; pause at Phase 15 when actual deployment prerequisites or approval are required. A paused or incomplete Goal 1 does not authorize starting Goal 2.
- Do not commit a phase that is incomplete or blocked.
- Do not invent a substitute for missing institution policy, credentials, or deployment details.
- After each goal's final phase passes its gate, report completion and stop. Do not add unrequested features or proceed beyond that goal's phase boundary or the roadmap.

## Goal Mode objectives

### Goal 1 — Phases 8–10

> Complete ARKTIESIIS roadmap Phases 8–10 in order: secure document management, local Tesseract/Poppler OCR, and advisory checks with manual review. Preserve the fixed project stack and all user changes. Enforce the documented role and document rules on the server. Complete each thesis implementation gate, report a concise checkpoint, and create one local commit for each completed phase. For Phase 9, require `npm test`, `npm run check`, `npm run db:check`, a successful real native `npm run ocr:smoke` on the local Linux environment, and `ARKTIESIIS_RUN_NATIVE_OCR_INTEGRATION=1 node --test tests/documentProcessing.test.js` on Linux. The opt-in integration check uses synthetic files and in-memory SQL harnesses; it verifies the private upload-to-processing path and transient Form 137 service but does not exercise live SQL Server or the authenticated route end to end. Run `npm run db:migration:rollback-test` only if migration 006 and its target tables are absent; if already installed, record the guarded refusal without changing the database. Windows executable-path unit tests and the historical Windows report are compatibility evidence only; they do not replace Linux checks. Never push. Use the thesis-owned Phase 10 checklist and status defaults in the plan; do not call them institution-approved. Keep institution signoff as a separate school-adoption gate pending before school deployment. Do not invent further institution policy. Stop after Phase 10 passes its thesis implementation gate and report school adoption as pending; do not begin Phase 11.

### Goal 2 — Phases 11–15

> Goal 1 has completed Phases 8–10 and passed its Phase 10 thesis implementation gate. Start a separate Goal Mode run in the same local checkout at Phase 11. Complete roadmap Phases 11–15 in order: dashboards/reporting, security/audit, testing, UI polish, and deployment/defense readiness. Preserve all existing and uncommitted changes. Keep the Node.js 20+, Express, EJS, HTML/CSS/JavaScript, SQL Server, local Tesseract/Poppler, and email-2FA stack. Enforce the documented server-side role and document rules. Work through each phase gate with relevant tests/checks, security review, and documentation updates; after each completed gate, report a concise checkpoint, create one local commit with a plain human message, and continue. Never push or commit an incomplete/blocked phase. School-adoption signoff remains pending; do not invent institution policy or describe thesis defaults as school-approved. Do not automatically provision infrastructure, spend money, apply production migrations, or deploy. At Phase 15, pause and report the missing prerequisites or approval if an actual external or irreversible action is required. Stop after Phase 15 passes its gate.

## Copy/paste prompts for the two new chats

Prompt 1 describes the completed Goal 1 run. Start Prompt 2 as a separate Goal Mode run now that the Phase 10 thesis implementation gate has passed. Keep both goals in the same local checkout; do not overlap runs.

### Prompt 1 — Goal 1, Phases 8–10

```text
Start Goal 1 using the Goal 1 objective in docs/08-phase-8-to-15-goal-plan.md. Work in this ARKTIESIIS repository and this same local checkout; do not create a fresh worktree. First read AGENTS.md, docs/07-paper-objectives-scope-limitations.md, and docs/08-phase-8-to-15-goal-plan.md. Phases 1–7 are complete; implement Phases 8–10 in roadmap order, using Node.js/Express/EJS/SQL Server/local Tesseract and Poppler/email-2FA. After each phase passes its acceptance gate, report a concise checkpoint and create one local commit for that phase. For Phase 9, run `npm test`, `npm run check`, `npm run db:check`, `npm run ocr:smoke`, and `ARKTIESIIS_RUN_NATIVE_OCR_INTEGRATION=1 node --test tests/documentProcessing.test.js`; the smoke and opt-in service integration must use installed Tesseract, English trained data, and Poppler on the local Linux environment. The integration check uses synthetic files and in-memory SQL harnesses; it does not exercise live SQL Server or the authenticated route end to end. Run `npm run db:migration:rollback-test` only if migration 006 and its target tables are absent; if already installed, record its guarded refusal without changing the database. Windows executable-path unit tests and the historical Windows report are compatibility evidence only and do not replace Linux checks. Never push. Follow the exact role matrix and security gates in the plan. Preserve all existing user changes. For each phase, make reviewable changes, run relevant checks, review access/security, and update docs as needed. Use the thesis-owned Phase 10 checklist and status defaults in the plan; do not call them institution-approved. Keep institution signoff as a separate school-adoption gate pending before school deployment. Do not invent further institution policy. Never claim OCR proves authenticity or detects forgery. After Phase 10 passes its thesis implementation gate, report Goal 1 complete with school adoption pending and stop. Do not begin Phase 11 or Goal 2.
```

### Prompt 2 — Goal 2, Phases 11–15

```text
Goal 1 completed Phases 8–10 and passed the Phase 10 thesis implementation gate. Start this separate Goal Mode run at Phase 11 in the same ARKTIESIIS checkout; do not create a worktree. First read AGENTS.md, docs/07-paper-objectives-scope-limitations.md, and docs/08-phase-8-to-15-goal-plan.md.

Implement Phases 11–15 sequentially. Preserve all existing and uncommitted changes. Keep the Node.js 20+, Express, EJS, HTML/CSS/JavaScript, SQL Server, local Tesseract/Poppler, and email-2FA stack, and enforce the documented server-side role and document rules. For each phase, make reviewable changes, run relevant tests/checks, review security/access, and update docs. After each passing gate, report a concise checkpoint, create one local commit with a plain human message, and continue. Never push or commit an incomplete/blocked phase.

School-adoption signoff remains pending; do not invent policy or describe thesis defaults as approved. This does not block local work in Phases 11–14. Do not automatically provision infrastructure, spend money, apply production migrations, or deploy. At Phase 15, pause and report if real external prerequisites or approval for an irreversible action are required. Never claim OCR proves authenticity or detects forgery. Stop after Phase 15 passes its gate.
```

## Out of scope

- Training a new AI model, document-type classification, or automated claims that a document is genuine.
- Forgery detection, signature/seal authentication, paper/material authenticity checks, or forensic analysis.
- Changing the fixed Node.js/Express/EJS/SQL Server/local Tesseract and Poppler/email-2FA stack.
- Adding multi-school support, unapproved AI capabilities, or features beyond roadmap scope.

## Inputs required before production validation

- Institution-approved required fields and format/compliance rules for Form 137, report cards, Good Moral Certificates, and PSA birth certificates.
- Confirmation of which validation cases require registrar review and what human outcomes/decision wording the system should record.
- Confirmation of upload size and retention limits. The repository currently has a configurable 10 MB default; that value is not asserted as school policy.
- Installed Tesseract with English trained data and installed Poppler command-line tools in the deployment environment.
